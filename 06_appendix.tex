% Replace the red placeholders with final implementation details before
% submission.
\providecommand{\appplaceholder}[1]{\textcolor{red}{\textbf{[Placeholder: #1]}}}

\subsection{RL Training Details}
\label{app:rl_training_details}

\appplaceholder{Report the complete actor and critic observation vectors,
including their dimensions, normalization, observation noise, and privileged
inputs. Specify the actor--critic architecture and all PPO hyperparameters,
including the optimizer, learning rate, discount factor, GAE parameter, clip
range, entropy and value-loss coefficients, batch size, rollout length, number
of environments, update schedule, and training duration. Provide the numerical
weights and definitions of every reward and penalty term in
Eq.~\eqref{eq:teacher_reward}, together with the initialization and domain
randomization settings used for teacher training.}

\subsection{VLM Inference Details}
\label{app:vlm_inference_details}

\appplaceholder{Provide the complete system and user prompts, exact model and
version identifier, access date, decoding and reasoning settings, and API
configuration. Define every rollout statistic supplied to the VLM and specify
the number of images, camera views, resolution, preprocessing, and image-selection
procedure used for each query. Include the machine-readable output schema,
parsing and validation logic, threshold ranges, clipping and smoothing rules,
query schedule, retry and timeout behavior, malformed-output handling, and
deterministic fallback. Report the measured latency, token usage, and monetary
cost per call and per training run.}
