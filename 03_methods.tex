% \begin{figure*}[t]
%     \centering
%     \includegraphics[width=0.8\linewidth]{figures/pipeline_screenshot.png}
%     \caption{Overview of the proposed teacher--student learning
%     pipeline. A privileged reinforcement learning teacher is
%     trained in simulation and subsequently distilled into an
%     RGB-based visuomotor policy for real-world deployment.}
%     \label{fig:pipeline}
% \end{figure*}

% \subsection{Problem Formulation}
% We consider online interactive imitation learning with teacher intervention in a
% partially observable Markov decision process (POMDP)
% $\mathcal{M}=(\mathcal{S},\mathcal{A},\mathcal{O},P,\Omega,r,\gamma)$, with
% privileged teacher state $s_t\in\mathcal{S}$, deployable student observation
% $o_t\in\mathcal{O}$ (RGB), action $a_t\in\mathcal{A}$, observation model
% $o_t\sim \Omega(\cdot\mid s_t)$, and dynamics $s_{t+1}\sim P(\cdot\mid s_t,a_t)$.
% During training, we assume access to a set of privileged teacher policies
% $\{\pi_{T_i}\}_{i=1}^{K}$, whereas at deployment the student policy $\pi_S$ must
% act solely from $o_t$. Our goal is to distill the collective teacher behavior
% into an RGB-based student that achieves high task success while minimizing
% unsafe exploration during training.

% \begin{equation}
% \begin{aligned}
% \min_{\substack{\pi_S\in\Pi_S\\ \mathcal{G}\in\mathfrak{G}}}
% \quad
% \Bigg(
% &
% \underbrace{
% -\mathbb{E}_{\tau\sim p_{\pi_{\mathcal{G}}}}\!\left[\mathbb{I}_{\mathrm{succ}}(\tau)\right]
% }_{\text{maximize success}}
% ,\;
% \\[-1pt]
% &
% \underbrace{
% \mathbb{E}_{\tau\sim p_{\pi_{\mathcal{G}}}}\!\left[
% \sum_{t=0}^{T-1}\mathbb{I}_{\mathrm{unsafe}}(s_t,a_t)
% \right]}_{\text{minimize unsafe}}
% \Bigg)
% \\
% \text{s.t.}\quad
% &
% a_t = g_t\, a_t^{T} + (1-g_t)\, a_t^{S},
% \\
% &
% g_t = \mathcal{G}(s_t,o_t)\in\{0,1\},
% \\
% &
% s_{t+1}\sim P(\cdot\mid s_t,a_t),\qquad o_t\sim \Omega(\cdot\mid s_t).
% \end{aligned}
% \label{eq:standard_form_safedistill_pareto_succ}
% \end{equation}
% where $g_t=1$ indicates teacher takeover and $g_t=0$ denotes student control, and
% $p_{\pi_{\mathcal{G}}}$ is induced by the gated rollout policy
% $\pi_{\mathcal{G}} \triangleq g_t\,\pi_T(\cdot\mid s_t) + (1-g_t)\,\pi_S(\cdot\mid o_t)$
% with $g_t=\mathcal{G}(s_t,o_t)$.
 
% We formalize task success via a trajectory-level predicate $\mathbb{I}_{\mathrm{succ}}(\tau)\in\{0,1\}$, which is $1$ if the
% object is grasped, lifted, and held above a height threshold for a consecutive duration.
% Safety is governed by early termination conditions: letting $\mathbb{I}_{\mathrm{unsafe}}(s_t,a_t)\in\{0,1\}$ indicate whether any unsafe event
% (e.g., harmful collision, object escape, palm flip, hand-too-far) occurs at time $t$, the trajectory is safe if
% $\prod_{t=0}^{T-1}\big(1-\mathbb{I}_{\mathrm{unsafe}}(s_t,a_t)\big)=1$.

% Training data are generated by a combined controller that switches between the evolving student and the privileged teachers.
% At each timestep, a binary gate $g_t\in\{0,1\}$ assigns control to a teacher ($g_t=1$) or to the student ($g_t=0$).
% When the teacher is selected, the takeover action is sampled from a teacher $\pi_T\in\{\pi_{T_i}\}_{i=1}^{K}$ (chosen by a fixed object
% index). Equivalently, the executed action is drawn from the mixture distribution
% \begin{equation}
% \begin{aligned}
% a_t &= g_t\, a_t^{T} + (1-g_t)\, a_t^{S},\\
% a_t^{T} &\sim \pi_T(\cdot \mid s_t), \qquad a_t^{S} \sim \pi_S(\cdot \mid o_t),
% \end{aligned}
% \label{eq:mixture_controller}
% \end{equation}
% which induces an intervened trajectory distribution $p_{\pi_\beta}(\tau)$ over $\tau=(s_{0:T},o_{0:T},a_{0:T-1})$.

% In our implementation, the intervention mechanism $\mathcal{G}$ is instantiated by an OR-rule combining teacher--student disagreement and a learned
% risk predictor. We compute teacher--student disagreement as an $\ell_2$ norm
% $d_t=\lVert a_t^{T}-a_t^{S}\rVert_2$ and use a predictor $\hat Q(s_t,a)\in[0,1]$ that estimates the probability of future task success,
% where lower values indicate higher risk. Teacher takeover is triggered by

% \begin{equation}
% g_t=\mathbb{I}\!\left[\hat Q(s_t,a_t^S)<\tau_{\mathrm{success}} \;\lor\; d_t>\tau_D\right]. 
% \end{equation}


% \subsection{Overview of the Distillation Pipeline}
% Our distillation pipeline incorporates an intervention mechanism that
% considers two aspects: state riskiness and teacher student disagreement. During rollout,
% teacher intervention is instantiated by an OR-rule (equ X).
% We summarize the overall distillation pipeline with risk-guided
% teacher intervention in Alg.~\ref{alg:safe_dagger}.

% \input{alg/alg01_intervention_structure}

% % ==========================

% \subsection{Safety sensitive policy distillation for dexterous manipulation}
% The training of the student policies is formulated as a
% supervised learning process. The student receives proprioceptive observations
% identical to actor state of the teacher policy, extended with two stereo RGB images 
% captured from the left and right cameras. 

% Training jointly minimizes an action imitation loss and an auxiliary prediction loss
% with $\mathcal{L}=\mathcal{L}_{\text{action}}+\mathcal{L}_{\text{aux}}$.
% The auxiliary loss penalizes object position prediction error,
% $\mathcal{L}_{\text{aux}}=\lVert \hat{\mathbf{x}}_{\text{obj}}-\mathbf{x}_{\text{obj}}\rVert_2$,
% where $\mathbf{x}_{\text{obj}}$ is the ground-truth object position and
% $\hat{\mathbf{x}}_{\text{obj}}$ is the image encoder prediction.

% For action imitation, we minimize a weighted $L_2$ loss between the student and teacher actions.
% Let $\mathbf{a}_{S} \sim \pi_S(\cdot \mid o_t)$ and $\mathbf{a}_T\sim \pi_S(\cdot \mid o_t)$ denote the student and teacher
% action outputs. The action loss is defined as
% \begin{equation}
% \mathcal{L}_{\text{action}}
% =
% \left\lVert \mathbf{a}_S - \mathbf{a}_T \right\rVert_{W}^{2}
% =
% (\mathbf{a}_S-\mathbf{a}_T)^\top W (\mathbf{a}_S-\mathbf{a}_T),
% \end{equation}
% where $W=\operatorname{diag}(w_1,\dots,w_d)$ is a diagonal weight matrix.
% We set $w_j=\sigma_{0,j}^{-2}$ using a reference scale $\sigma_{0,j}$
% (e.g., the teacher's action standard deviation for dimension $j$), yielding
% \begin{equation}
% \mathcal{L}_{\text{action}}
% =
% \sum_{j=1}^{d} w_j \left(a_{S,j}-a_{T,j}\right)^2
% =
% \sum_{j=1}^{d} \frac{\left(a_{S,j}-a_{T,j}\right)^2}{\sigma_{0,j}^{2}}.
% \end{equation}
% This normalization emphasizes matching the teacher more tightly along action dimensions
% with smaller intrinsic scale while avoiding over-penalizing dimensions with larger scale.

% The distillation training pipeline is divided into two stages. In the first stage, we warm-start 
% training by collecting an offline dataset of 100k data points. This dataset is used to 
% pretrain a risk predictor that estimates a score representing the contribution of the 
% visited states to task success. Teacher policies always take control at this stage.

% In the second stage, training is governed by online rollouts under a teacher-intervened 
% policy. The teacher takes over control of the executed action based on a mixed 
% intervention mechanism. The details of the intervention mechanism are described below:

% %=================================================
% \subsection{Privileged teacher policy training}
% Teacher policy training is formulated as a reinforcement learning (RL) problem with the transition tuple
% $\langle s_t, a_t, r_t, s_{t+1} \rangle$, where $s_t$ is the state at timestep $t$, $a_t$ is the action,
% $r_t$ is the reward, and $s_{t+1}$ is the next state. The objective is to learn a policy that maximizes
% the expected discounted return. We use a shaped reward $r_t=\sum_{k=1}^{K_r} r_{t,k}$ to encourage stable
% grasp acquisition, object lifting, and goal reaching, where $r_{t,k}$ denotes the $k$-th reward term
% (Table~X).

% \begin{equation}
% \pi_\theta^{*}
% =
% \arg\max_{\pi_\theta}\;
% \mathbb{E}_{\pi_\theta}
% \left[
% \sum_{t=0}^{T-1}
% \gamma^t r_t
% \right].
% \label{eq:teacher_rl_objective}
% \end{equation}

% The teacher policy is trained in simulation using an asymmetric actor--critic framework. In this setup,
% the actor operates on noisy and partial proprioceptive observations that approximate realistic inputs,
% while the critic receives full state information of both the robot and the object, available only in
% simulation.

% \input{table/reward_shaping.tex}

% Let $\mathbb{I}_{\mathrm{term}}(s_t)\in\{0,1\}$ denote the termination indicator, which captures both
% timeout and early-termination conditions. The episode terminates at
% \begin{equation}
% T = \min \left\{ t \,\middle|\, \mathbb{I}_{\mathrm{unsafe}}(s_t)=1 \;\; \text{or} \;\; t \ge T_{\max} \right\}.
% \end{equation}

% Timeout termination defines the task horizon with a default duration of $T_{\max}=10\,\mathrm{s}$.
% To encourage more controlled and safe exploration, we additionally introduce early termination during
% training.

% The early termination indicators are activated when any of the following
% conditions is satisfied:
% \begin{itemize}
%     \item \textbf{Palm flipped}
%     ($\mathbb{I}_{\mathrm{flip}}$): the palm rotates beyond a
%     predefined angular threshold, indicating a failed
%     approach behavior.
    
%     \item \textbf{Harmful collision}
%     ($\mathbb{I}_{\mathrm{coll}}$): hand or arm links collide
%     with the table, representing unsafe interactions for
%     delicate dexterous hand hardware.
    
%     \item \textbf{Object escape}
%     ($\mathbb{I}_{\mathrm{obj}}$): the object leaves a predefined
%     workspace boundary, indicating grasp failure caused by
%     pushing rather than grasping.
    
%     \item \textbf{Hand too far}
%     ($\mathbb{I}_{\mathrm{hf}}$): the hand origin moves outside
%     the workspace region above the table, suggesting
%     exploration of task-irrelevant areas.
% \end{itemize}

% The above termination conditions are also treated as sources of 
% unsafe behavior for subsequent teacher intervention decisions.


% \paragraph{Teacher--student disagreement.}
% We measure teacher--student disagreement using the difference between the teacher and student action outputs.
% Let $\mathbf{a}_S$ and $\mathbf{a}_T$ denote the student and teacher actions, respectively.
% The disagreement score is defined as the $\ell_2$ norm of their action difference,
% $d(t)=\lVert \mathbf{a}_S - \mathbf{a}_T \rVert_2$.

% The disagreement score is compared against a predefined threshold
% $\tau_D$ to determine whether the current state is novel to the student.
% If $d(t)>\tau_D$, the state is considered novel, indicating that the
% student is uncertain about the appropriate action and may benefit from
% teacher intervention.

% \subsection{Risk predictor}
% The risk prediction mechanism is inspired by ThriftyDAgger and is
% implemented as a dual-critic risk estimator trained with
% SARSA-style Bellman targets to estimate future task success. 
% The risk predictor is implemented as a clipped double-critic architecture containing $Q_1$ and $Q_2$ with
% sigmoid outputs in $[0,1]$, interpreted as predicted
% success probabilities. The final estimate is computed using clipped
% double evaluation,
% $\hat Q_(s,a)=\min(Q_1(s,a),Q_2(s,a))$,
% yielding a conservative estimate of future success.

% The predictor is pretrained during a warm-start phase using offline
% transitions $(s_t,a_t,s_{t+1},a_{t+1},u_t,d_t,h_t)$, where $s_t$ and
% $a_t$ denote the current state and executed action, and
% $(s_{t+1},a_{t+1})$ provide the subsequent state--action pair required
% for SARSA-style target computation. The variable $u_t\in\{0,1\}$ denotes
% an immediate success label, $d_t$ represents the episode termination
% signal, and $h_t$ specifies whether the next action $a_{t+1}$ is valid.
% The inclusion of $(s_{t+1},a_{t+1})$ together with $h_t$ enables
% consistent bootstrapping of future success estimates while preventing
% invalid updates when the next action is undefined at episode boundaries.

% The predictor is subsequently updated online during distillation, where
% new transitions collected from student rollouts are continuously appended
% to a ring replay buffer to adapt the estimator to the evolving student
% policy distribution.

% Training follows a SARSA-style update with an effective termination
% signal $d_t^{\mathrm{eff}}=\max(d_t,1-h_t)$, which treats transitions
% without a valid next action as terminal. The target value is computed as


% \begin{equation}
% \small
% y_t =
% u_t + \gamma(1-d_t^{\mathrm{eff}})
% \min_{i\in\{1,2\}}
% Q_i^{\mathrm{targ}}(s_{t+1},a_{t+1}).
% \end{equation}

% where the bootstrapped term propagates future success estimates only
% through valid non-terminal transitions. Clipping preserves a
% probabilistic interpretation of the predicted success value.

% Both critics are optimized using weighted mean-squared error
% $\mathcal{L}_{Q_i}=\mathbb{E}[\,w_t(Q_i(s_t,a_t)-y_t)^2\,]$ for
% $i\in\{1,2\}$, with total loss
% $\mathcal{L}=\mathcal{L}_{Q_1}+\mathcal{L}_{Q_2}$.
% Target networks are updated via Polyak averaging
% $\theta'\leftarrow\rho\theta'+(1-\rho)\theta$ with $\rho=0.995$.

% \subsection{Intervention mechanism}
% During intervention decision, teacher takeover is triggered when the predicted
% success estimate falls below a predefined threshold,
% $\hat Q_(s,a)<\tau_{\mathrm{success}}$, indicating a high-risk state which contribute
% little to the task success. Intuitively, disagreement detects distribution shift, while the risk predictor anticipates downstream failures even when the action discrepancy is small.


% =========================
%  Methods (re-structured)
% =========================

\begin{figure*}[t]
    \centering
    \includegraphics[width=0.8\linewidth]{figures/fig02_network_structure.pdf}
    \caption{Overview of the proposed teacher--student learning pipeline. A
    privileged reinforcement learning teacher is trained in simulation and
    subsequently distilled into an RGB-based visuomotor policy for real-world
    deployment.}
    \label{fig:pipeline}
\end{figure*}

\subsection{Problem Formulation}
We consider online interactive imitation learning with teacher intervention in a
partially observable Markov decision process (POMDP)
$\mathcal{M}=(\mathcal{S},\mathcal{A},\mathcal{O},P,\Omega,r,\gamma)$, with
privileged teacher state $s_t\in\mathcal{S}$, deployable student observation
$o_t\in\mathcal{O}$ (RGB), action $a_t\in\mathcal{A}$, observation model
$o_t\sim \Omega(\cdot\mid s_t)$, and dynamics $s_{t+1}\sim P(\cdot\mid s_t,a_t)$.
During training, we assume access to a set of privileged teacher policies
$\{\pi_{T_i}\}_{i=1}^{K}$, whereas at deployment the student policy $\pi_S$ must
act solely from $o_t$. Our goal is to distill the collective teacher behavior
into an RGB-based student that achieves high task success while minimizing
unsafe exploration during training.

% \begin{equation}
% \small
% \begin{aligned}
% \min_{\substack{\pi_S\in\Pi_S\\ \mathcal{G}\in\mathfrak{G}}}
% \quad
% \Bigg(
% &
% \underbrace{
% -\mathbb{E}_{\tau\sim p_{\pi_{\mathcal{G}}}}\!\left[\mathbb{I}_{\mathrm{succ}}(\tau)\right]
% }_{\text{maximize success}}
% ,\;
% \\[-1pt]
% &
% \underbrace{
% \mathbb{E}_{\tau\sim p_{\pi_{\mathcal{G}}}}\!\left[
% \sum_{t=0}^{T-1}\mathbb{I}_{\mathrm{unsafe}}(s_t,a_t)
% \right]}_{\text{minimize unsafe}}
% \Bigg)
% \\
% \text{s.t.}\quad
% &
% a_t = g_t\, a_t^{T} + (1-g_t)\, a_t^{S},
% \\
% &
% g_t = \mathcal{G}(s_t,o_t)\in\{0,1\},
% \\
% &
% s_{t+1}\sim P(\cdot\mid s_t,a_t),\qquad o_t\sim \Omega(\cdot\mid s_t).
% \end{aligned}
% \label{eq:standard_form_safedistill_pareto_succ}
% \end{equation}

\begin{equation}
\small
\begin{aligned}
\min_{\substack{\pi_S\in\Pi_S\\ \mathcal{G}\in\mathfrak{G}}}
\quad
&
\Bigg[
\underbrace{
-\mathbb{E}_{\tau\sim p_{\pi_{\mathcal{G}}}}\!\left[\mathbb{I}_{\mathrm{succ}}(\tau)\right]
}_{\text{maximize success}}
\;+\;
\lambda\,
\underbrace{
\mathbb{E}_{\tau\sim p_{\pi_{\mathcal{G}}}}\!\left[
\sum_{t=0}^{T-1}\mathbb{I}_{\mathrm{unsafe}}(s_t,a_t)
\right]}_{\text{minimize unsafe}}
\Bigg]
\\
\text{s.t.}\quad
&
a_t = g_t\, a_t^{T} + (1-g_t)\, a_t^{S},
\\
&
g_t = \mathcal{G}(s_t,o_t)\in\{0,1\},
\\
&
s_{t+1}\sim P(\cdot\mid s_t,a_t),\qquad o_t\sim \Omega(\cdot\mid s_t).
\end{aligned}
\label{eq:standard_form_safedistill_pareto_succ}
\end{equation}

where $g_t=1$ indicates teacher takeover and $g_t=0$ denotes student control. The state distribution
$p_{\pi_{\mathcal{G}}}$ is induced by the gated rollout policy $\pi_{\mathcal{G}}$, which switches between the
teacher and student policies according to the intervention gate $g_t=\mathcal{G}(s_t,o_t)$.

We formalize task success via a trajectory-level predicate
$\mathbb{I}_{\mathrm{succ}}(\tau)\in\{0,1\}$, which is $1$ if the object is
grasped, lifted, and held above a height threshold for a consecutive duration.
Safety is governed by early termination conditions: letting
$\mathbb{I}_{\mathrm{unsafe}}(s_t,a_t)\in\{0,1\}$ indicate whether any unsafe
event (e.g., harmful collision, object escape, palm flip, hand-too-far) occurs
at time $t$, the trajectory is safe if
$\prod_{t=0}^{T-1}\big(1-\mathbb{I}_{\mathrm{unsafe}}(s_t)\big)=1$.

\subsection{Overview of the Distillation Pipeline}
Fig.~\ref{fig:pipeline} summarizes our teacher--student distillation pipeline,
which consists of three components: (i) privileged teacher policy training in
simulation (Sec.~C), (ii) risk predictor learning to estimate lift success
under student actions (Sec.~D), and (iii) online student distillation with
teacher intervention (Secs.~E--F). We warm-start the risk predictor using an
offline dataset collected under full teacher control and continue updating it
periodically during online distillation using mini-batches sampled from a ring
replay buffer. During interactive rollouts, an intervention gate $\mathcal{G}$
switches control between teacher and student based on an OR-rule combining a
learned risk estimate and teacher--student action disagreement. The student is
then optimized online using per-timestep teacher labels collected under the
gated rollout policy $\pi_{\mathcal{G}}$. Pseudo-code is provided in
Alg.~\ref{alg:safe_dagger}.

\input{alg/alg01_intervention_structure}

\subsection{Privileged Teacher Policy Training}
Teacher policy learning is formulated as a reinforcement learning problem with
transition tuples $\langle s_t,a_t,r_t,s_{t+1}\rangle$, where $s_t$ is the
privileged state, $a_t$ is the action, $r_t$ is the reward, and $s_{t+1}$ is the
next state. The objective is to maximize the expected discounted return,
\begin{equation}
\small
\pi_\theta^{*}
=
\arg\max_{\pi_\theta}\;
\mathbb{E}_{\pi_\theta}
\left[
\sum_{t=0}^{T-1}\gamma^t r_t
\right].
\label{eq:teacher_rl_objective}
\end{equation}
We use a shaped reward $r_t=\sum_{k=1}^{K_r} r_{t,k}$ to encourage stable grasp
acquisition, object lifting, and goal reaching (Table \ref{tab:reward_terms}).

The teacher is trained in simulation using an asymmetric actor--critic setup (Table \ref{tab:teacher_obs}):
the actor receives noisy and partial proprioceptive observations that
approximate realistic sensing, while the critic is provided with privileged full
state of both robot and object, available only in simulation.

\input{table/reward_shaping.tex}

Episodes terminate at
\begin{equation}
\small
T = \min\left\{t \,\middle|\, \mathbb{I}_{\mathrm{unsafe}}(s_t)=1 \;\;\text{or}\;\; t \ge T_{\max}\right\},
\end{equation}
where $T_{\max}=10\,\mathrm{s}$ is the timeout horizon. Early termination
conditions include palm flip, harmful collision, object escape, and hand-too-far
(see Sec.~A), and are treated as unsafe events for both teacher training and
subsequent intervention decisions.

% \subsection{Risk Predictor}
% Action disagreement alone cannot anticipate downstream failures, particularly when the student action is locally
% similar to the teacher action yet triggers unstable contact transitions. To address this limitation, we learn a risk
% predictor that conservatively estimates the probability of \emph{lift success}. The predictor is used only during
% training to guide teacher intervention and is not required at deployment time.

% The risk predictor is designed to estimate risk from multimodal inputs. It takes as input a state--action pair
% $(s_t,a_t)$, where $s_t$ comprises robot proprioceptive features concatenated with image embeddings produced by a
% visual encoder to provide semantic information. The visual encoder is trained end-to-end during distillation and
% regularized via an auxiliary object-position prediction loss (Sec.~F). The action $a_t$ is the
% \emph{executed} action at time $t$ (teacher or student, depending on the intervention gate). 
% Following a clipped double-critic design, we maintain two critics $Q_1$ and $Q_2$ implemented as ReLU MLPs with hidden
% sizes $[256,128]$, each outputting a scalar logit mapped to a success probability via a sigmoid; we then compute
% \begin{equation}
% \small
% \hat Q(s,a)=\min\!\left(Q_1(s,a),\,Q_2(s,a)\right),
% \end{equation}
% which provides a conservative estimate of lift success (lower values indicate higher risk).


% The predictor is pretrained in a warm-start phase using an offline dataset of
% transitions $(s_t,a_t,s_{t+1},a_{t+1},u_t,d_t,h_t)$. Here, $(s_t,a_t)$ and
% $(s_{t+1},a_{t+1})$ provide the current and next state--action pairs required by
% SARSA-style bootstrapping, $d_t\in\{0,1\}$ is the episode termination flag, and
% $h_t\in\{0,1\}$ indicates whether the next action $a_{t+1}$ is defined (valid)
% for bootstrapping. The label $u_t\in\{0,1\}$ encodes \emph{safe lift-event}
% attainment by time $t$: $u_t=1$ if the object has been lifted above a height
% threshold for a consecutive duration (as in $\mathbb{I}_{\mathrm{succ}}$) and no
% unsafe event has occurred up to time $t$; otherwise $u_t=0$.

% During online distillation, newly collected transitions are appended to a ring
% (sized $100k$) at every environment step. The predictor critics are updated 
% periodically: every $500$ environment steps, we
% perform $20$ gradient updates using mini-batches sampled from the buffer using 
% Adam with learning rate $10^{-3}$.

% Training uses SARSA-style bootstrapped regression with an effective termination
% signal $d_t^{\mathrm{eff}}=\max(d_t,1-h_t)$ and one-step target
% \begin{equation}
% \small
% y_t =
% u_t + \gamma(1-d_t^{\mathrm{eff}})
% \min_{i\in\{1,2\}}
% Q_i^{\mathrm{targ}}(s_{t+1},a_{t+1}),
% \end{equation}
% where $Q_i^{\mathrm{targ}}$ are slowly-updated target networks (Polyak averaging)
% used only to stabilize bootstrapping, and the $\min$ operator mitigates
% overestimation. The critics are optimized with MSE
% $\mathcal{L}_{Q_i}=\mathbb{E}\!\left[\,w_t\big(Q_i(s_t,a_t)-y_t\big)^2\,\right]$,
% with total loss $\mathcal{L}=\mathcal{L}_{Q_1}+\mathcal{L}_{Q_2}$ and target
% updates $\theta'\leftarrow\rho\theta'+(1-\rho)\theta$ ($\rho=0.995$).

% During interactive distillation, the predictor estimates the task success probability
% of executing the \emph{student} action: given $s_t$ and
% $a_t^{S}\sim\pi_S(\cdot\mid o_t)$, we compute $\hat Q(s_t,a_t^{S})$ and trigger
% teacher takeover when it falls below a threshold (Sec.~E).

\subsection{Risk Predictor}
Action disagreement alone cannot anticipate downstream failures, particularly when the student action is locally
similar to the teacher action yet triggers unstable contact transitions. To address this limitation, we learn a risk
predictor that conservatively estimates the probability of \emph{lift success}. The predictor is used only during
training to guide teacher intervention and is not required at deployment time.

The risk predictor is designed to estimate risk from multimodal inputs. It takes as input a state--action pair
$(s_t,a_t)$, where $s_t$ comprises robot proprioceptive features concatenated with image embeddings produced by a
visual encoder to provide semantic information. The visual encoder is trained end-to-end during distillation and
regularized via an auxiliary object-position prediction loss (Sec.~F). The action $a_t$ is the
\emph{executed} action at time $t$ (teacher or student, depending on the intervention gate).
Following a clipped double-critic design, we maintain two critics $Q_1$ and $Q_2$ implemented as ReLU MLPs with hidden
sizes $[256,128]$, each outputting a scalar logit $z_i(s,a)\in\mathbb{R}$ mapped to a success probability via a
temperature-calibrated sigmoid, $Q_i(s,a)=\sigma\!\left(z_i(s,a)/T\right)$ for $i\in\{1,2\}$, where $T=4$ is an empirically set calibration temperature. We then compute
\begin{equation}
\small
\hat Q(s,a)=\min\!\left(Q_1(s,a),\,Q_2(s,a)\right).
\end{equation}

which provides a conservative estimate of lift success (lower values indicate higher risk).

The predictor is pretrained in a warm-start phase using an offline dataset of
transitions $(s_t,a_t,s_{t+1},a_{t+1},u_t,d_t,h_t)$. Here, $(s_t,a_t)$ and
$(s_{t+1},a_{t+1})$ provide the current and next state--action pairs required by
SARSA-style bootstrapping, $d_t\in\{0,1\}$ is the episode termination flag, and
$h_t\in\{0,1\}$ indicates whether the next action $a_{t+1}$ is defined (valid)
for bootstrapping. The label $u_t\in\{0,1\}$ encodes \emph{safe lift-event}
attainment by time $t$: $u_t=1$ if the object has been lifted above a height
threshold for a consecutive duration (as in $\mathbb{I}_{\mathrm{succ}}$) and no
unsafe event has occurred up to time $t$; otherwise $u_t=0$.

During online distillation, newly collected transitions are appended to a ring buffer
(sized $100k$) at every environment step. The predictor critics are updated
periodically: every $500$ environment steps, we
perform $20$ gradient updates using mini-batches sampled from the buffer using
Adam with learning rate $10^{-3}$.

Training uses SARSA-style bootstrapped regression with an effective termination
signal $d_t^{\mathrm{eff}}=\max(d_t,1-h_t)$ and one-step target
\begin{equation}
\small
y_t =
\mathrm{clip}\!\left(
u_t + \gamma(1-d_t^{\mathrm{eff}})
\min_{i\in\{1,2\}}
Q_i^{\mathrm{targ}}(s_{t+1},a_{t+1}),
\,0,\,1\right),
\end{equation}
where $Q_i^{\mathrm{targ}}$ are slowly-updated target networks (Polyak averaging)
used only to stabilize bootstrapping, and the $\min$ operator mitigates
overestimation. The critics are optimized with MSE
$\mathcal{L}_{Q_i}=\mathbb{E}\!\left[\,w_t\big(Q_i(s_t,a_t)-y_t\big)^2\,\right]$,
with total loss $\mathcal{L}=\mathcal{L}_{Q_1}+\mathcal{L}_{Q_2}$ and target
updates $\theta'\leftarrow\rho\theta'+(1-\rho)\theta$ ($\rho=0.995$).

During interactive distillation, the predictor estimates the task success probability
of executing the \emph{student} action: given $s_t$ and
$a_t^{S}\sim\pi_S(\cdot\mid o_t)$, we compute $\hat Q(s_t,a_t^{S})$ and trigger
teacher takeover when it falls below a threshold (Sec.~E).

\subsection{Intervention Mechanism}
At each timestep, we compute (i) a risk score using the learned predictor and
(ii) a teacher--student disagreement score. Disagreement is defined as
$\delta_t \coloneqq \|a_t^{S}-a_t^{T}\|_2$
where $a_t^{T}\sim\pi_T(\cdot\mid s_t)$ and $a_t^{S}\sim\pi_S(\cdot\mid o_t)$ are
the teacher and student action outputs. The risk criterion flags states whose
predicted success under the student action is low, i.e.,
$\hat Q(s_t,a_t^{S})<\tau_{\mathrm{success}}$.

We instantiate the intervention gate $\mathcal{G}$ as an OR-rule that triggers
teacher takeover when either the predictor indicates high risk or the student
deviates substantially from the teacher:
\begin{equation}
\small
g_t=\mathbb{I}\!\left[\hat Q(s_t,a_t^{S})<\tau_{\mathrm{success}} \;\lor\; d_t>\tau_D\right].
\label{eq:gate_or_rule}
\end{equation}
Intuitively, disagreement detects distribution shift, while the risk predictor
anticipates downstream failures even when the instantaneous action discrepancy
is small.

We set $\tau_D$ to the $90$th percentile of the teacher--student disagreement
$\delta_t$ measured on the warm-start dataset. We set $\tau_{\mathrm{success}}$ to a
low percentile (e.g., the $10$th) of $\hat Q(s_t,a_t^{T})$ computed over
successful warm-start rollouts. Both thresholds are kept fixed throughout
training. These percentiles are chosen empirically to balance intervention frequency
and exploration efficiency.

\subsection{Student Policy Distillation}
Student learning is posed as supervised imitation from teacher-labeled
transitions collected under the gated rollout policy $\pi_{\mathcal{G}}$. The
student observes RGB images (two stereo views) together with robot proprioception.

We optimize a combined objective
$\mathcal{L}=\mathcal{L}_{\text{action}}+\mathcal{L}_{\text{aux}}$, where the
auxiliary loss predicts object position to regularize the visual encoder,
$\mathcal{L}_{\text{aux}}=\lVert \hat{\mathbf{x}}_{\text{obj}}-\mathbf{x}_{\text{obj}}\rVert_2.$

For action imitation, we use a weighted $L_2$ loss,
$\mathcal{L}_{\text{action}}
=\lVert \mathbf{a}_S-\mathbf{a}_T\rVert_W^2
=(\mathbf{a}_S-\mathbf{a}_T)^\top W(\mathbf{a}_S-\mathbf{a}_T).$
with $W=\mathrm{diag}(w_1,\dots,w_d)$. We set
$w_j=\sigma_{0,j}^{-2}$ using a reference scale $\sigma_{0,j}$ (e.g., the
teacher action standard deviation on dimension $j$), yielding
$\mathcal{L}_{\text{action}}=\sum_{j=1}^{d}(a_{S,j}-a_{T,j})^2/\sigma_{0,j}^{2}.$

Training proceeds in two stages. (i) \emph{Warm-start:} we collect an offline
dataset (e.g., $100$k transitions) under full teacher control ($g_t\equiv 1$) to
pretrain the risk predictor. (ii) \emph{Online
distillation:} we perform interactive rollouts under $\pi_{\mathcal{G}}$ and
query teacher labels on intervened steps. The student is updated online using
the current-step teacher label only; we do \emph{not} maintain a replay buffer
or DAgger-style aggregated dataset for student updating. The design reduces the 
effect of stale supervision and stabilizes online imitation updates.